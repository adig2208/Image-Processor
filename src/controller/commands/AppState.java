package controller.commands;

/**
 * Defines the various states of the application in relation to image handling.
 */
public enum AppState {
  NO_IMAGE_LOADED,
  IMAGE_LOADED_UNMODIFIED,
  IMAGE_LOADED_MODIFIED
}
